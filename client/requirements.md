## Packages
(none needed)

## Notes
Uses existing shadcn/ui components in client/src/components/ui
Dark-mode first: default to .dark class on <html> (handled in ThemeProvider component)
Seed endpoint POST /api/admin/seed is triggered from Dashboard when engine list is empty
All fetches use credentials: "include"
